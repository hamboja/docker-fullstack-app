language: generic

sudo: required

services:
  - docker

before_install:
  - docker build -t hambojajob0308/react-test-app -f ./frontend/Dockerfile.dev ./frontend

script:
  - docker run -e CI=true hambojajob0308/react-test-app npm test

after_success:
  - docker build -t hambojajob0308/docker-frontend ./frontend
  - docker build -t hambojajob0308/docker-backend ./backend
  - docker build -t hambojajob0308/docker-nginx ./nginx

  - echo "$DOCKER_HUB_PASSWORD" | docker login -u "$DOCKER_HUB_ID" --password-stdin

  - docker push hambojajob0308/docker-frontend
  - docker push hambojajob0308/docker-backend
  - docker push hambojajob0308/docker-nginx

# deploy:
#   provider: elasticbeanstalk
#   region: "ap-northeast-2"
#   app: "docker-fullstack-app"
#   env: "DockerFullstackApp-env"
#   bucket_name: elasticbeanstalk-ap-northeast-2-972153559337
#   bucket_path: "docker-fullstack-app"
#   on:
#     branch: main
  
#   access_key_id: $AWS_ACCESS_KEY
#   secret_access_key: $AWS_SECRET_ACCESS_KEY